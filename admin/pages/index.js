import Layout from "../components/Layout";

export default function AdminHome() {
  return (
    <Layout>
      <h1>Admin Panel (Starter)</h1>
      <p>This is a minimal admin shell. Replace later with the official Medusa Admin or custom pages.</p>
      <ul>
        <li>Products — list/create/edit (coming soon)</li>
        <li>Orders — list/status (coming soon)</li>
        <li>Customers — list/details (coming soon)</li>
      </ul>
    </Layout>
  );
}
